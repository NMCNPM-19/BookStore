<div class="content-page">
    <div class="content">

        <!-- Start Content-->
        <div class="container-fluid">

            <!-- start page title -->
            <div class="row">
                <div class="col-12">
                    <div class="page-title-box">
                        <h4 class="page-title">Edit Product</h4>
                    </div>
                </div>
            </div>
            <!-- end page title -->
        </div>
        <!-- Form Edit Product -->
        <div id="editProductModal">
            <div class="modal-dialog">
                <div class="modal-content">
                    <form method="POST" action="/products/saveUpdate/{{product.masach}}?_method=PUT"
                        enctype="multipart/form-data">
                        <div class="modal-header">
                            <h4 class="modal-title">Update Product</h4>
                            <a href="/products">
                                <button type="button" class="close" data-dismiss="modal"
                                    aria-hidden="true">&times;</button>
                            </a>
                        </div>
                        <div class="modal-body">
                            <div class="form-group">
                                <label>Tên Sách</label>
                                <input type="text" class="form-control" value="{{product.tensach}}" name="tensach" required>
                            </div>
                            <div class="form-group">
                                <label>Tác giả</label>
                                <input type="text" class="form-control" value="{{product.tacgia}}" name="tacgia" required>
                            </div>
                            <div class="form-group">
                                <label>Ngày xuất bản</label>
                                <select class="form-control" id="NXB" name="manxb">
                                    {{#each NXB}}
                                    {{{option this.manxb this.Ten ../product.manxb }}}
                                    {{/each}}
                                </select>
                            </div>
                            <div class="form-group">
                                <label>Ngày xuất bản</label>
                                <input type="date" class="form-control" value="{{product.ngayXB}}" name="ngayXB" required>
                            </div>
                            <div class="form-group">
                                <label>Mô tả</label>
                                <textarea class="form-control" cols="86" rows="10" placeholder="Mô tả truyện"
                                    name="MOTA" id="des">{{product.MOTA}}</textarea>
                            </div>
                            <div class="form-group-3">
                                <label>Giá</label>
                                <input type="number" class="form-control" value="{{product.gia}}" name="gia" required>
                            </div>
                            <div class="form-group">
                                <label>Ảnh</label><br>
                                <img src="{{product.IMAGE}}" alt="{{product.IMAGE}}" width="500" height="600"><br>
                                <input type="url" class="form-control" value="{{product.IMAGE}}" readonly>
                                <p style="margin-top: 10px;">Thay đổi: </p>
                                <input type="file" name="image">
                            </div>
                        </div>
                        <div class="modal-footer">
                            <input type="submit" class="btn btn-info" value="Save">
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <!-- End Form Edit Product -->
    </div>
</div>